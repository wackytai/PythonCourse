if 1 and 1:
	# the following statement would be a conditional inside an if:
	# so it stops at the first false value and returns. It is known as
	# -- Short-circuit evaluation --
	print(True and 1 and False)